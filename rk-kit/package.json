
{
  "name": "@eva/rk-kit",
  "description": "Processing tool for react-native-ui-kitten",
  "version": "0.0.1",
  "license": "MIT",
  "author": "akveo <contact@akveo.com>",
  "homepage": "https://github.com/akveo/eva#readme",
  "repository": "git+https://github.com/akveo/eva.git",
  "bugs": {
    "url": "https://github.com/akveo/eva/issues"
  },
  "scripts": {
    "start": "npm run build -- --watch",
    "build": "tsc",
    "generate": "( cd ./scripts && ts-node generate-index )",
    "clean": "rimraf ./dist",
    "lint": "tslint --project tsconfig.json ./src/*.ts",
    "lint:fix": "npm run lint -- --fix",
    "ci:test": "npm test -- --coverage && cat ./dist/jest/coverage/lcov.info | coveralls",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^23.3.11",
    "@types/node": "^10.12.18",
    "husky": "^1.1.2",
    "jest": "^24.0.0-alpha.9",
    "rimraf": "^2.6.3",
    "ts-jest": "^23.10.5",
    "ts-node": "^7.0.1",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}